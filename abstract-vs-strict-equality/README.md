## Javascript Abstract Equality sign ```==``` vs Strict Equality sign ```===``.
They are both comparison signs. The ```===``` can be used to compare both values and types whereas ```==``` can be used to only compare values.

